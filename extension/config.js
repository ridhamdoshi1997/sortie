// Single place to point the extension at Sortie's deployed app. Swap this
// one value to test against a local dev server instead of production.
const SORTIE_API_BASE = "https://jobpilot-experiment.vercel.app";
